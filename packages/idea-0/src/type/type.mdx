import { Meta } from "@storybook/blocks";

<Meta title="type" />;

# iDea에서 사용되는 타입을 설명하는 문서입니다.

## Primitive

```
type Primitive = string | number | boolean;
```

## Formatter

```typescript
type Formatter<T extends Primitive> = (
  value: T,
  key?: string,
  index?: number
) => ReactNode;
```
