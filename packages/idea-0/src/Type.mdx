import { Meta } from "@storybook/blocks";

<Meta />

# iDea에서 사용되는 타입을 설명하는 문서입니다.

## ValueData

```typescript
type ValueData = string | number | boolean;
```

## EntryData

```typescript
type EntryData = [string, ValueData];
```

## Formatter

```typescript
type Formatter<T extends ValueData> = (
  value: T,
  key?: string,
  index?: number
) => ReactNode;
```
